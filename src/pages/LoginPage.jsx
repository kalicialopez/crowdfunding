import LoginForm from "../components/LoginForm/LoginForm";

function LoginPage() {
  return (
    <body className="page-body">
      <h1>Login to create campaigns and make pledges</h1>
      <LoginForm />
    </body>
  );
}

export default LoginPage;
